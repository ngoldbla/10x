# Nine 2.0 — "The Deep End" Program Plan

> The program of record for Nine 2.0. Per-PRD specs live beside it as
> `nine/PRD-<n>.md`; how to actually execute one is
> [docs/EXECUTING-A-PRD.md](docs/EXECUTING-A-PRD.md). The plan body below is
> unchanged from the approved version — only the status table is maintained.

## Execution status (updated 2026-07-30)

| Item | State | Notes |
|---|---|---|
| **Wave 0 backlog** | | |
| PRD-6 Watch | **6a shipped, merge-blocked on provisioning** | Map → box-lens → Crown rose, one `BoardView` at two camera positions. The daily arrives over WatchConnectivity because it composes at steady and the watch may not compose above gentle — so the link is load-bearing, and a test fails the day those two constants agree. The link carries an immutable puzzle down and a `PendingSolve` up, never play state: `SharedDailyBoard`'s own safety proof is that both writers only append, and a wrist has undo. Four defects found only by installing it, starting with `WKCompanionAppBundleIdentifier` — which the plan said was unnecessary and `simctl install` said was mandatory, after a green build. Not done: 6b complications, any real hardware (so Double Tap has never fired), KVS unverifiable in a simulator, no watch AX or contrast lane. **Cannot merge until the portal App ID exists and `match` is re-minted** — embedding a new bundle changes the iOS archive's profile set |
| PRD-11 coach hints + auto-notes | written, **not shipped** | UI substrate for PRD-25 |
| PRD-12 share card | **shipped** | Glass card (1080×1350 PNG) one tap from the completion chip, iOS + macOS. `ShareCard` is generic over its body at `ShareCardMetrics.bodySide` and `SolveCardFacts` holds every caption — PRD-26's comet replaces the body at the same frame, captioned from the same value. Two defects found only by driving it: a `@State` read inside `TimelineView`'s escaping closure, and a cancellable 2.4 s sleep. Not done: tvOS (no share sheet), image thumbnail in the sheet |
| PRD-13 streak grace | **shipped** | One missed day bridges; two always break. `displayedStreak` deviates from §2's literal wording, which composed with §2's own non-stacking rule would have flipped a 12-day chip to 1 at the instant of a successful solve. Shield replaces the flame (GlassChip is single-glyph). Found a third copy of the streak rule in `BoardIntents` that PRD-13 made shame the player. Not done: tvOS/macOS grace card (glyph only) |
| PRD-14 daily archive | written, **not shipped** | replay entry surface for PRD-26 |
| PRD-15 feedback | **half shipped** | haptics landed as PRD-21; sound still blocked on assets |
| PRD-16 themes/icons | **shipped** | Ember/Tide/Mono + Moss/Orchid + three alternate iOS icons, no locks. The palette is a pinned contract now: it fired on its first run against unmodified code (crimson/blueprint 4.23:1, shipped, grandfathered to PRD-22) and it rejected the obvious ninth accent — a periwinkle in the glacier→lilac gap cannot clear Blueprint's blue ground and stay clear of Lilac at once, so a hue gap on the wheel is not a usable gap. Alternate icons need **no entitlement and no match re-mint**, proved by an Info.plist diff, and need **two** asset-catalog settings — ALTERNATE_APP_ICON_NAMES alone is a silent no-op that three green platform builds do not catch. Not done: the AX lane lost the accent swatches to the taller theme row (PRD-19's harness), composited-glass contrast still PRD-22's |
| PRD-17 Nocturne | **shipped** | fourth band: Sharp's chain at a 26-clue floor with ≥3 advanced steps. Compose p95 **5.25 s** (200 seeds, Mac **Release** — and the discovery that every prior compose figure in DEVIATIONS was a ~50× slower Debug number). Phase 0 paid off: the library needed no change, `nine.history` did — a `band` sibling key, because the throwing build is already on TestFlight. Downgrade drill scripted against a real checkout and falsified. Not done: a device-measured compose number, Nocturne dailies |
| PRD-18 welcome + teaser + UXDemo deletion | **shipped** | welcome ledger (iOS), variants teaser with a 2026-10-25 remove-by date, both `UXDemo` files deleted — every prototype scene is `git show a18cbe3:…` away (PRD-7 rule 4). Not done: tvOS/macOS welcome parity |
| **Wave 1 — "Worthy"** | | |
| Phase 0 foundations | **partly shipped** | element-level quarantine + 56-pair golden corpus in; PRD-17 extended the quarantine to `nine.history` and scripted the downgrade drill (§3). Field-level preservation implemented, measured at 1515 ms vs 49 ms, reverted — see DEVIATIONS |
| PRD-19 accessibility | **shipped** | 81 AX children in 9 box containers (Switch Control group scan), rotors, actions, 44 pt chrome, Voice Control cell names, `describe-ui` baselines diffed per screen on every PR. Not done: a tvOS/macOS lane (`describe-ui` is iOS-only), and the tvOS board still has no per-cell actions |
| PRD-20 localization | **shipped; no human has read the nine languages** | That status line was written before anyone counted. The infrastructure was ~397 strings across 48 files — of which only 11 had the `Phrase` block `EXECUTING-A-PRD.md` describes as "the single seam", and most reached the screen as `String` through helpers and the non-localizing `.accessibilityLabel` overload, so Xcode's own extraction saw almost none of it. Catalog is **425 keys × 10 locales**; call-site offences **134 → 0**; corpus **56/56 after every task**. `techniqueID` was never needed — the raw values were already the frozen identity, so the Engine change is a deletion of three computed properties. Five claims turned out false only when run, including `xcstringstool` accepting a plural with no `other` (exit 0, renders `(null)`, defeating the English fallback) and a plural axis that selected on the *digit* instead of the count — inverted, in English, today. Three pre-existing bugs fixed: English weekday initials in every locale, "1 day streak", and a Mac/iPhone caption that disagreed about the same board. The pseudo-loc/RTL lane shipped and **found two bugs on its first honest run**: the board's 81 accessibility frames mirrored under RTL while the `Canvas` pixels did not (`Row 1, column 1` reported at x=342 with the digit 4 drawn at x=20 — every cell but column 5 in the wrong place for VoiceOver, invisible to a screenshot diff), and the rose mirrored *visibly* to `3 2 1 / 6 5 4 / 9 8 7`, which is exactly the harm decision 3 forbids. Both were pinned with `.environment(\.layoutDirection, .leftToRight)`. The lane's specified truncation rule was measured before it was built and **could never have fired** — zero accessibility labels in this app contain an ellipsis, because long text wraps and SwiftUI reports the full string — so it asserts `(null)` renders, unsubstituted specifiers, horizontal overflow and the RTL invariants instead, and records wrapping as a reviewable baseline rather than an error. Not done: **no human has read any of the nine languages** (the headline deferral), no tvOS/macOS loc lane (`describe-ui` is iOS-only), and `MacUI.swift` still undriven — a locally-built Nine cannot launch on macOS at all on an iCloud-signed-in host, trapping in `CKContainer.init` because the entitlement-free build passes an account check it should not. **Translators are still the open item — that part was true.** The **Dynamic Type lane was retired rather than deferred**: it was specified as an AX5 sweep, and `Sources/App` holds 0 `@ScaledMetric`, 0 `dynamicTypeSize` and 0 `relativeTo:` against 64 fixed `.system(size:)` and 107 `CouchTypography` references — nothing grows, so the sweep would have measured its own absence and returned green. Making Nine scale means changing `CouchTypography`, which all five suite apps render through: **[PRD-36](PRD-36.md)**. The plan's predicted first failure (six `GlassIconButton`s wanting `.contentShape(.accessibility, Circle())`) had already been fixed by PRD-19 at `TouchUI.swift:1558` — stale in both directions. |
| PRD-21 audio + haptics | **haptics shipped** | audio blocked: no recorded samples exist |
| PRD-22 lens | **shipped** | Contrast is measured on the composited glass now, not computed from constants the board never draws: `scripts/contrast-harness.py` screenshots a seeded simulator per cell and samples pixels. Solving the box washes back out recovered the plane — Void's `(0,0,0)` reaches the board as **(19,19,19)**, Camel's tan as **(250,230,200)** — and the harness fired on its first run against unmodified code: Camel entries 3.36:1, Camel coral 1.92:1, and PRD-16's *one* grandfathered crimson pair turning out to be **five** dark themes below AA, not one. Crimson moved to the closest tint (ΔE 5.84) that clears Blueprint without colliding with Orchid under tritanopia — the obvious brighter candidate does, at ΔE 3.77. All ten light-accent variants deepened; coral became a theme tone. The plane wash is **zero on light themes, measured**: every light-theme mark is darker than its ground, so restoring the ground costs contrast (Camel givens 10.28 → 8.15). Petals are true glass — a third stitchable function on the board Canvas derives nine lens centres from the ring's own pitch. Reduce Motion keeps today's material. Not done: tvOS/macOS have no contrast lane (`describe-ui` is iOS-only), the light variants' dichromat separation is pinned at ΔE 3.17 rather than fixed, and the tvOS rose is still unclamped |
| PRD-34 first five minutes | **shipped** | new-game routing, drawer grabber, prefs regroup, and now the first run: welcome ledger → one pre-focused cell, "flick to place a 7", skippable from the first frame. Tip budget capped at three ever / one per session, hand-rolled rather than TipKit (see DEVIATIONS). Not done: the Mac has no first run at all |
| PRD-23 variant engine + Killer | **engine shipped, sealed** | Constraint architecture + Killer generation, engine only, behind a channel with no user-facing surface (Release cannot reach it; a test greps the app layer and fails if it does). Classic held byte-identical through all five commits — 56/56 after each, `Sudoku`/`BacktrackSolver` untouched, `GeneratedPuzzle` gained no field. Compose p95 Mac Release: **gentle 0.02 s / steady 0.05 s / sharp 0.14 s**, 100/100 each; Sharp is zero-given. Sharp only composes at all because cage *size* — not technique coverage — was the binding constraint (0 of 200 zero-given boards are unique at maxCageSize 5). Not done: device number, any persistence, catalogs/pantry, thermo supply, chute rule-of-45 |
| [PRD-26](PRD-26.md) The Comet | **shipped** | `LoggedMove.at` is caller-passed and Optional, so the engine still reads no clock and an untimed log's bytes do not move. Replays are immutable, packed (300 timed moves = **1288 bytes**), in their own blob, pruned with the library and synced as a new record type in the zone that already exists — **no entitlement change, no `match` re-mint**; a production schema deploy is the one human gate. The classifier took three attempts, and the two dead ends are the finding: "does technique T *place* this cell" can only ever answer `hiddenSingle` (every pair, fish and wing eliminates), and "run the chain until the cell falls" credits an X-Wing on the far side of the grid — on a board *proved solvable by logic* that also makes `.leap` unreachable. PRD-25's `derivation`, reused, is the answer. The comet is never unbidden; the debrief is a pull-up mirroring the stats drawer at the opposite edge; the share chip stays **one** chip carrying a 5 s H.264 loop or PRD-12's still, because three real paths mint no log at all. Untimed logs replay with no tell and the debrief drops its two timing lines rather than fabricating them. Four defects found only by driving it and one only by `ax-snapshot.py` — a rotor action on all 81 cells of every unsolved board, because `accessibilityAction(named:)` registers whatever the closure guards. Not done: a macOS debrief (a pull-up is a touch gesture), tvOS debrief or share (no share sheet there), a device-measured export, and the nine languages are machine-drafted |
| [PRD-31](PRD-31.md) The Drafting Table (iPad) | **shipped** | The composition is a pure function of the **window**, never of the device — Linux-clean in `Sources/Shared`, swept over 19,097 window sizes — and that *is* the Stage Manager story: there is no Stage Manager code. A size class would be actively wrong (a 1000×700 tile reports `.regular` with less usable width than an iPhone has height). The adoption rule is "the rail never costs you board", which is what correctly refuses portrait, where any width threshold admitting an 11" landscape also drops an upright Pro from 984pt to 568. `MacBoardKeys` moved out of its `#if os(macOS)` whole, so keyboard parity is mostly the deletion of a platform fence. Pencil handwriting is the release's one new input concept and it spends nothing else: erase is `togglePencil` toggling, so writing a digit twice removes it — no mode, no gesture, no glyph. Recognition is $P against **authored** templates that never learn (a matcher fed its own mistakes degrades toward them); the specimen is per *player*, **773 bytes** for nine glyphs, so every pencil mark wears your hand including the ones the rose places on a phone. Both constants were measured rather than chosen: the commit bar sits above the worst *wrong* reading (0.391) and knowingly refuses some right ones, because the populations overlap; a margin bar was written, measured redundant and **deleted**, with the test that fails if it stops being. Three defects found only by driving an iPad — a `minHeight`-with-no-maximum card inflating to half the screen in a second column, a rail that was 200pt of stats above 700pt of empty glass, and keyboard parity that built green and could not receive a keystroke because `.focusable()` sat *inside* the `GeometryReader` (`MacUI` has had those lines outside its own since PRD-4, under a comment about focus wars). AX baselines all five match, no re-record. Not done: **no Pencil has ever written a digit and no pointer has ever hovered** — neither exists in a simulator, so the recognizer is measured only against constructed strokes (the *rendering* half was driven, seeded and photographed); no Mac rail; no handedness pref; no iPad AX lane; and `sim-use tap` still cannot drive the shelf, re-confirmed against unmodified `main` |
| [PRD-30](PRD-30.md) Quiet Presence | **shipped** | A Live Activity with no clock in it, enforced by the shape of the payload rather than by a comment: `DailyPresence` carries a day, a band id, a **22-byte** glyph and a revision, and the dynamic half carries only the last two. Two seals hold the negative — one reflects over the encoded JSON keys, one greps every quiet surface for `timerInterval`, `style: .timer`, `AlertConfiguration` and the word "streak", because `Text(_:style: .timer)` needs no field in the payload at all. **No entitlement and no `match` re-mint**, checked against `CoreBuildSystem.xcspec` rather than assumed: this is the first new surface in three PRDs that needs nothing from the portal. The fence is `#if os(iOS)` and not `canImport(ActivityKit)`, which is true on macOS and then fails on the conformance. StandBy has no API at all — it is a `WidgetLocation` you can only opt *out* of — so the face is `systemSmall` with its container background removed, an inference named in the source. Fixed a bug three shipped widgets had: `SharedAppearance` crossed to the watch in PRD-6 but never to the widget extension, so the Home Screen has been hardcoded glacier-on-paper since PRD-3. Not done: **no Live Activity has ever appeared on a Lock Screen** — the simulator lane broken on 2026-07-29 is still broken — no Dynamic Island hardware, machine-drafted languages |
| [PRD-33](PRD-33.md) Nine, Everywhere You Ask | **shipped** | Four App Shortcuts, a Focus filter, a medium interactive widget, and the Mac's first real menu bar. **Journaling Suggestions cannot be built**: the framework is 350 lines of *consumer* API with fifteen closed asset types and no `donate` anywhere in the SDK; the one path that would work (HealthKit `HKStateOfMind`) costs an entitlement, a permission prompt and asking a player how they feel about a sudoku. App Intents cannot use `Strings.resource(_:)` — `appintentsmetadataprocessor` is a static extractor — and its error is **not fatal to the compile**, so the first build shipped zero shortcuts with `** BUILD SUCCEEDED`; the artifact is the only honest check. Fixed PRD-20's standing gap: `CKContainer.init` **traps** an unentitled build, so `mayBuildCloudContainer` asks `SecTask` instead of asking the OS about an account — and everything Mac-side in this PRD was driven because of it. Then two obvious spellings of `MenuBarExtra(isInserted:)` — an `@Observable` read and `@AppStorage` — pinned a core at **103% CPU** in `scenesDidChange` → `makeMainMenu` → `requestUpdate`, green on three platforms and 615 tests, caught only by driving it. Three "missing" Mac features were one `#if os(iOS)` each. The literal "three moves" cap was refused as paywall-shaped; the widget's one real configuration parameter is **handedness**, which PRD-31 deferred as too expensive for a settings row. Not done: no phrase spoken, no Focus activated, widget migration unverified, no Mac first run/drawer/debrief |
| [PRD-24](PRD-24.md) Channels | **shipped (iOS)** | Classic \| Thermo \| Killer, each with its own Today, streak, stats slice and leaderboard; dailies one per day per channel. Thermo first, and the case for it is measured: **200/200 per tier at a p95 of 0.01–0.03 s** Mac Release, 5–14× cheaper than killer Sharp and ~175× cheaper than Nocturne. "The classic streak is never diluted" is a **type**, not a comment — `Channel.Ledgered` has no classic case, so writing a classic solve into `nine.channels` is not a call that can be written; `nine.streak`/`nine.history`/`nine.archive` are byte-identical and the drill asserts it. `StreakState` and `SolveHistory` are reused once per channel, so PRD-13's grace and every stats query arrived already written. The first `thermoBand` **composed 200/200 and was still wrong**: `maxGivens` 30/24/18 against a measured max of 19/19/11 and `minVariantSteps` 3/6/10 against a p50 of 11/13/18 — neither knob could ever reject, so the tier was defined by whatever the dig did. What walks the ladder is chain width (a wider chain closes with fewer givens: 14 → 12 → 7), which answers PRD-23 §5's open question: killer's ladder is compressed, thermo's is not. `GameKind.channel` quarantining on an old build is the **desired** outcome rather than a tolerated one — a build with no cage renderer would mark correct entries as errors — which inverts every other tolerance rule in the repo. PRD-23's seal was **re-aimed rather than deleted**: `VariantInputSealTests` seals the input layer and the wrist, which is permanent, where the old one asserted a fact that expires on ship day. Three defects found only by driving it, all green everywhere else: the pager's leading chevron was **absent from the AX tree** (a label on a `.contain` container merged it away — the trap `TouchUI.swift:308` documents), the tier cards reported a 41pt AX frame under the 44pt floor, and **the tubes were silent** — `Row 3, column 5 / Empty` with nine thermometers on screen. That last fix's finding: the clause belongs in the *label*, because VoiceOver hints can be turned off and a cage sum is not a tip. PRD-20's lane refused the diff **five times, every time correctly**, including a `%N$@` splice inside a `coach.*` sentence. Variant generation is now frozen (`VariantCorpusTests`, 9 seeds) — PRD-23 rightly needed no corpus, and one line of product spec made it mandatory. Not done: **no tvOS or macOS channel page** (a page-turn is a touch gesture; both stay on Classic and neither is broken), **variant boards do not sync** (needs a CloudKit record type + schema deploy, so a board without its rules refuses to open rather than opening wrong), **the per-channel leaderboards need ASC records** (a human gate; no entitlement, so **no `match` re-mint**), no channel archive, no widget channel parameter, no variant-board contrast fixture, share card says the tier not the channel, and the nine languages are machine-drafted |
| [PRD-28](PRD-28.md) The Parlor | **shipped (iOS), merge-blocked on provisioning** | The same seeded board on every device and nothing else on the wire: no shared grid, no authority, no state to reconcile — the hardest problem in multiplayer is deleted rather than solved, and the golden corpus becomes load-bearing in a second direction (a quiet change hands two friends different boards under one invite). "No times until everyone finishes" is a **type**, not a rule a view obeys: `ParlorPresence` is `{fill, done}` with no time field, and `Member.finish` is nil until the room completes, so no surface can draw a number it is not handed. Sealed twice — encoded keys *and* a source grep — and the source seal was falsified before it was trusted. **The program plan's Game Center sentence could not be built as written**: every classic challenge API is deprecated as of iOS 26 and its replacement carries no payload, so the seed rides `GKGameActivity.properties`, which is why the invite has two envelopes and a tested `[String: String]` codec. A strict codec was written and reverted — ASC's `defaultProperties` merges keys into every activity, so refusing unknown keys would have refused every real invite; the **version** is the guard. SharePlay will not name a participant (`Participant` is one `UUID`), so anonymous dots are what the framework can support rather than a taste choice. Two AX defects on one card, both found only by driving: 64×64 with the content shape on the glyph, then 298×**33** without it — under the 44 pt floor, PRD-24's 41 pt defect again. Fixed `DuelHandoff.decide`, which shipped as `{ .continue }` in PRD-27 and left `main` **7-of-8 red**. Not done: **the Group Activities entitlement is a real portal capability and a merge blocker** (checked against `DVTPortalCachedPortalCapabilities.json`, where PRD-30 got a *no* and this gets a *yes*); no `GKGameActivityDefinition` record, so the send chip is absent and that whole path is unrun; **no real group session has ever existed on this lane** (no FaceTime between simulators, and these are slimmed with `messaging` off) so everything is driven through a DEBUG loopback; the side-by-side comets have never been on a screen; no tvOS/macOS/watch parlor; no variant channels; no persistence at all |
| [PRD-29](PRD-29.md) The Table | **shipped (iOS, iPadOS, macOS, tvOS)** | Twenty people, one week, consistency first and time second — and the design is what fell out of reading the framework rather than working around it. **Game Center cannot partition players**: there is no API that assigns you to a cohort, only `loadEntries(for:timeScope:range:)` over *absolute rank positions* plus a local-player entry that always comes back. So a table is a **window** on the recurring board, not a cohort in it, and every negative the covenant wanted stops being a promise — there is no Table 3, so there is nothing to be relegated from, and a delta needs two observations where Nine keeps one. The whole persisted footprint is **one `Bool`**, which is why "you dropped four places" is a message no code path can construct. The plan's CloudKit-public-DB fallback was therefore **not built**. The ranking rule is one line of arithmetic packed into the single `Int64` a leaderboard carries, invertible so every drawn row reads both numbers off the entry and `context` can stay 0; its clamp's lower bound is **1 and not 0**, because at zero the score equals `(days + 1) × timeSpan` — a day of consistency manufactured by arithmetic, on a path where untimed solves really do record 0. Total and average seconds induce the identical order (a tie is an equal day count is an equal denominator), so the cheaper one is correct. The week is Monday by construction — ordinal 0 is 2001-01-01 — and `Calendar.firstWeekday` is deliberately overruled, the one place Nine does that: two players counting different windows against one occurrence are playing different games and neither can tell. Anti-cheat is `BacktrackSolver` + `ReplayWalk` reused, six findings each falsified, and **one of the six cannot fire on any caller that exists** — the packed format stores unsigned deltas — which is stated in the spec, the doc comment and the test name rather than collecting a green tick, PRD-24's `thermoBand` lesson applied to a check instead of a knob. Its reach is written down too: it runs on the player's own device against their own replay, so it is no defence against a patched binary and there is no server to move it to. Absence of a replay is **not** a finding (widget and watch solves mint none) and an untrusted day drops the day, not the week. The 44 pt floor failed a **third** time (36 pt, after PRD-24's 41 and PRD-28's 33) and needed both `minHeight` *and* the shape on the `Button` rather than its label. The History sheet had **no AX baseline at all** — added as a seventh screen; the six existing match with no re-record. The constructed demo standing was unsorted, which reads exactly like a real ordering bug, so it sorts by the real score now. Driven end to end by seeding four dailies into `nine.history` on device: `You, 4 of 7 days, 26:42`, seated below a faster four-day week and above a faster three-day one. All five seals falsified before trusted, including a **count** of `showsCompletionBanner` rather than a ban, because achievements legitimately set it in the same file the table submits from. Not done: **no App Store Connect recurring-leaderboard record**, so the whole GameKit half is unrun and a real table has never been on a screen (no entitlement, so **no `match` re-mint**; the record needs a Monday-anchored 7-day recurrence and best-score-per-occurrence, neither checkable in code); `tableOccurrence()` written and not yet consulted; no per-channel, friends, or past-week tables; no watch surface; no contrast fixture; table rows are 16 pt in the AX tree; and **`ja`/`ko`/`zh-Hans` have no register-neutral word for a standings table** — all three drafts name rank, the thing the English spent its design removing |
| **Wave 3 remainder** | PRD-24 + PRD-27 + PRD-28 + PRD-29 shipped | PRD-35 store re-launch remains. PRD-27 has no DEVIATIONS section and no row here, and its two surfaces (shelf card, hand-off card) were never built — read PRD-28's section before picking it up |

Shipped in PR #32 ("Worthy"; TestFlight tvOS 450 / iOS 451 / macOS 452). Every
deferral above is recorded with its reason in the "Phase 0" and "Worthy"
sections of [DEVIATIONS.md](DEVIATIONS.md) — read those before picking up any
half-finished PRD, because the omissions were decisions, not oversights.

## Context

Nine 1.1 — the $4.99 universal Couch Suite sudoku (iPhone/iPad, Mac, Apple TV, iOS widgets) — has shipped its founding vision: flick-rose input that never misfires, proof-checked puzzles, cloud library, rich stats, plus Wave 1 of the $4.99 buildout (PRD-8/9/10). The user asked for a v2.0 definition that fundamentally deepens **every surface the user experiences**, at the fidelity of a multi-year program by expert Apple-native teams.

This plan was produced from: (a) full codebase + PRD-corpus exploration, (b) a live sim-use audit of the latest build on an iPhone 17 Pro simulator (screenshots in `/tmp/nine-audit/`), and (c) three parallel design passes — product surfaces, engine/systems, craft/calm. Per the user's decisions: **pricing is deferred** (plan the product; the PRD-7 covenant — no IAP, no Pro tier, no ads — remains binding), **visionOS is out of scope**, and the deliverable is **this plan document only** (no in-repo PRDs yet).

### Program thesis

1.x proved the *input* covenant (the rose). 2.0 proves the *engine* covenant: the deterministic generator-verifier that emits human-readable proof traces is a platform, not a curiosity. Two shipped assets power nearly everything: the serialized `SolveStep` trace chain (`nine/Sources/Engine/LogicSolver.swift` — "why") and the move log (`nine/Sources/Engine/Game.swift` — "how"). 2.0 turns *why* into a Coach and a variant channel, *how* into replays and quiet multiplayer — while paying down the debts (accessibility, localization, audio, contrast, onboarding) that separate a good indie app from an Apple Design Award contender. Calm is the constraint on every line.

### What the live sim audit found (build 1.1)

Strengths verified: rose grammar (petal tap, erase petal, dimmed complete digits), same-number highlight, coral+dot error marking, pull-down stats drawer, playable tutorial, rich prefs (6 themes/8 accents), instant resume, live light↔dark switching.

Gaps verified (beyond the known PRD-6/11–18 backlog):
1. **The board exposes zero accessibility elements** — `describe-ui` shows no cells; VoiceOver users cannot play at all. Chrome AX frames are tiny (9×15pt chevron).
2. **No audio identity anywhere**; placement/error haptics exist as built patterns in `AfterglowHaptics.swift`/`AfterglowScoreTiming.swift` but are **never wired on iOS** (only the solve score plays).
3. **Zero localization** — every string hardcoded English, including engine `displayName`s.
4. **Dark-theme board contrast very low** (glass plane lifts the void; digits tuned against the raw theme constant, not the composited surface); tutorial sheet muddy in dark.
5. **Home shelf emptiness** — Today card a mostly-blank slab; Continue/Boards rows render blank gray rings at 0%.
6. **Discoverability debt** — stats drawer has no affordance (by design comment, but it fails in practice); "New game" buried at the bottom of Settings.
7. Rose petals are opaque `.glassEffect` discs — not the PRD's "true glass petals lensing the board beneath."
8. No onboarding; no iPad-specific composition (centered phone column); no Watch app.

### Standing backlog 2.0 builds on (wave 0 — commitments, not v2 novelty)

PRD-6 (Watch: box-zoom + Crown rose), PRD-11 (coach hints + auto-notes), PRD-12 (share card), PRD-13 (streak grace), PRD-14 (daily archive), PRD-15 (feedback haptics/chime), PRD-16 (themes/icons), PRD-17 (Nocturne difficulty), PRD-18 (welcome + variants teaser + UXDemo deletion). Loose ends: DualSense Create/Options verification (PRD-5), iCloud pace-skew fix (PRD-10 drawer), four open CouchKit asks (ambiguous-flick forwarding, click-vs-tap, play/pause double-fire, sheet focus hand-off).

---

## The Program: five pillars, PRD-19…35

### Pillar A — "Worthy" (the invisible expert-team work; ships early as a 1.5-class release)

- **PRD-20 — Nine in Nine Languages.** String Catalogs first (before the string explosion): mechanical extraction of every literal; Engine never localizes (Linux CI) — it emits stable IDs (`techniqueID`, difficulty raw values), a single App-layer `Strings.swift` maps IDs → `LocalizedStringResource`. Launch: JA, DE, FR, ES, IT, PT-BR, KO, zh-Hans, NL (sudoku's biggest markets are Japan and Germany). Pseudo-loc + RTL audit CI lane; plural variants; Dynamic Type stress tests.
- **PRD-19 — A Voice for the Board (Accessibility).** Keep the single Canvas; add virtual AX children: 9 box/row containers × cells, stable identities, value-only updates. A pure `BoardSpeech` formatter (Sources/Shared; unit-tested; shared with the coach's sentence templates): "Row 3, column 5 — your 4, error / empty, notes 2 5 9". Custom rotors (Empty cells, Conflicts, Notes, per-digit); the rose opens as an AX-modal 9-button ring (and/or adjustable element: swipe cycles digits, double-tap commits); post-move announcements ("Four placed. Two fours remaining."). Switch Control group-scan = boxes→cells; Voice Control cell addressing; Full Keyboard Access audit; 44pt AX frames on all chrome. CI: `describe-ui` AX-tree dumps diffed per screen.
- **PRD-21 — The Sound of Nine (Audio identity).** A `CalmAudio` module (candidate for couchkit): one `AVAudioEngine`, preloaded PCM buffers, all samples <120ms recorded glass/felt at −18dBFS with ±3-cent variation. Placement tick, pencil (low-passed twin), erase (reversed grain), error hush (quieter than placement — whispered, never scolded), digit-complete partial, and a 2.6s solve shimmer that tracks the Afterglow luminance wave exactly. Session `.ambient` + `.mixWithOthers` (never ducks the user's podcast — critical given the PiP feature); silent switch respected. Nothing sounds on navigation/home/rose-open. Simultaneously **wire the already-built placement/error/box-detent haptic patterns on iOS** with a persistent `CHHapticEngine` (20ms budget; AHAP with embedded audio for sample-accurate sync). Sync law: visual anchors; haptic within 20ms; audio within one frame of the haptic.
- **PRD-22 — The Lens (rendering).** True glass petals: SwiftUI `layerEffect` can't sample behind a layer, so glassifying petal views is a dead end — instead extend the **board Canvas's existing Metal pipeline** (Afterglow.metal already applies two `layerEffect`s) with a third per-petal refraction shader (9 centers + radius + magnification uniforms) so board digits genuinely bend and magnify under each petal; SwiftUI draws only glyphs and rims above. Reduce-Motion/fallback = current material (pure upside). Plus: **dark contrast retune** measured against the *composited* glass (standards: givens ≥7:1, entries ≥4.5:1, coral ≥3:1, across 6 themes × 8 accents × light/dark, sampled from screenshots in an automated harness); an `accessibilityContrast` hairline variant; **real board-fingerprint thumbnails** on the home shelf (deterministic seeds make them free — givens as dot constellation, filling with accent dots as you progress; no more blank rings; below ~3% show "Untouched", never a 0% ring); route the tutorial through the theme system (fixes the muddy dark composite).
- **PRD-34 — The First Five Minutes (onboarding + IA).** First launch *is* the tutorial's first beat: one pre-focused cell, "flick to place a 7", under 30 seconds, one-tap skippable (the PRD's own metric: superpower within 60 seconds). New Game leaves Settings entirely — its homes: the difficulty cards on the shelf, an "Another" action after Afterglow settles, and a "Fresh board" row atop the Boards sheet. Stats drawer gets a one-time 3pt hairline grabber that appears for the first three sessions then fades forever. TipKit hard-capped: three tips total, one per session max. Prefs regroup into Play / Feel (new: sound+haptics) / Appearance / Layout / About.

### Pillar B — "The Second Language" (variants — the generator-verifier's named payoff)

- **PRD-23 — Variant Engine: Killer (+ constraint architecture).** No protocol rewrite: `SudokuGrid` untouched; new Codable `VariantConstraint` (`.cage(cells:sum:)`, `.thermometer(cells:)`, later arrow/kropki/sandwich; unknown discriminators decode to `.unrecognized`) compiled once per puzzle into a `ConstraintContext` (extra peers, cage tables, thermo bounds; classic = shared static empty context, one pointer compare). `CandidateState`/solver techniques iterate `context.peers/units` which return the exact static classic arrays for classic — **byte-identical goldens prove no destabilization**. Variant reasoning enters as ordinary `Technique` cases (cageSingle, cageCombination, innieOutie/rule-of-45, thermoBound) emitting ordinary `SolveStep`s — the Coach speaks variants for free. `BacktrackSolver` gets a constraint-checked twin (originals frozen — they define classic dailies forever). Killer generation: seeded grid → seeded polyomino cage tiling → sums from solution → *add* givens (inverse dig-hole) until technique-bounded. Cost mitigations: **fast-seed catalogs** (ship proven-to-converge seeds, not puzzles — a few KB; device re-proves on demand), a background `PuzzleForge` pantry actor (2–3 proven puzzles per variant×tier, refilled on idle/charging), attempt budgets (never `while true` for variants). A variant tier ships only when catalog mining shows p95 compose within budget.
- **PRD-24 — Channels (Thermo + the variant shelf).** Thermo ships first as the de-risking variant (cheap generation; thermos rendered as luminous glass tubes). Product framing: **Channels** — home shelf page-turns between Classic | Killer | Thermo, each with its own Today, streak, stats slice, and leaderboard; dailies one-per-day per channel so the classic streak is never diluted. The rose is unchanged across variants — the input covenant is variant-agnostic, which is the strategic point. Watch stays classic-only. **AI-generated rulesets explicitly deferred to 2.x** (they strain the proof covenant until the verifier can gate arbitrary rules).

### Pillar C — "The Coach" (the traces become a teacher)

- **PRD-25 — Why Must This Be a Seven?** Long-press any empty cell (⌥-click Mac; long-press-select tvOS): the engine re-derives the minimal `SolveStep` sub-chain forcing that cell and narrates it *on the board* — involved cells breathe in sequence, one step at a time, no text walls. A **Technique School** extends the playable tutorial: one lesson per technique through the new ceilings, each a real seeded position (an exemplar = `(seed, difficulty, stepIndex)` ≈ 20 bytes; the device regenerates and proves it; CI validates every exemplar). Mastery tracked quietly (`CoachProgress`, <2KB, KVS). Hints (PRD-11) become "show me the next why." Solver expansion for the deep end: **Tempest** (swordfish via a generalized `fish(n:)` where X-Wing = `fish(2)` with byte-identical emitted steps, skyscraper, XY-Wing) and **Abyss** (simple coloring, optionally W-Wing; stop before full chains — explanation complexity is the limit). Trace schema v2 (additive, tolerant): stable `techniqueID` strings as the wire/l10n identity, `roles` (base/cover/pivot/victim) for coach rendering, `chain` links for coloring animation.
- **PRD-26 — The Comet (replay + debrief).** `LoggedMove` gains optional elapsed-time (`at:`, caller-passed — the engine still never reads a clock; old logs replay at uniform cadence). At solve time a finalized `SolveReplay` is minted (packed binary move log, 1–2KB per solve) — replay analysis re-runs the solver alongside your moves to classify each placement (matched-trace / harder-than-needed / guess), feeding stats and `CoachProgress`. UI: a comet retraces your solve — hesitations slow it, erasures loop retrograde; post-solve **Debrief** card (pull-up, never forced): fastest region, longest-circled cell, "you found the X-Wing at move 31." The 5s comet loop becomes the animated body of the PRD-12 share card. tvOS gets a your-own-solves ambient screensaver.

### Pillar D — "Together, Quietly" (multiplayer a calm brand survives)

- **PRD-27 — Pass the Remote (tvOS/iPad local duel).** Two players, one remote, one board, alternating fixed turns; per-player tint; errors corrected quietly at hand-off; move-log credits contributions in the debrief. No login, no network — App-layer only.
- **[PRD-28](PRD-28.md) — The Parlor (SharePlay + GC challenges).** Same seeded puzzle on each FaceTime participant's device; ambient presence only (a soft glow-dot per friend showing fill %, no times until everyone finishes; then side-by-side comets). Game Center challenges wrap the same primitive async: "beat my Thursday daily" sends a seed. Deterministic seeds are the transport — trivially correct everywhere.
- **[PRD-29](PRD-29.md) — The Table (daily league).** Opt-in 20-person weekly tables ranked by completion-consistency first, time second; no demotion shame; never notifies; rendered in the stats drawer's hand-inked Canvas language. Backed by Game Center recurring leaderboards (no server); CloudKit public-DB pods only if GC granularity can't express tables. Anti-cheat: pure-Engine replay re-simulation (legal moves, final grid = proven solution, monotone timing) — trust-but-verify, appropriate for a zen game.

### Pillar E — "Every Surface, Native Depth"

- **[PRD-30](PRD-30.md) — Quiet Presence.** Live Activity: mini board state after you start-and-leave the daily — **no timers, no countdowns, no streak-endangered nagging ever** (PRD-13 grace exists so we never have to). Dynamic Island: tiny board glyph only. StandBy: a dedicated ambient face — today's board as a nightstand glass object.
- **PRD-31 — The Drafting Table (iPad).** True regular-width composition: board center-stage, stats drawer becomes a persistent right rail (discoverability solved by geometry), controls float beside the board; Stage Manager/external display sanity; Mac hover-halo on trackpad; keyboard parity. Apple Pencil: hover previews the petal under the tip; strokes become pencil marks *in your handwriting* (same hand-drawn Canvas language as stats) — the 2.0 signature-moment candidate. (Craft rule tension noted: this is the release's one new input concept.)
- **PRD-32 — reserved (visionOS)** — explicitly out of scope per user decision; slot kept to avoid renumbering if revisited in 2.x.
- **[PRD-33](PRD-33.md) — Nine, Everywhere You Ask.** Full App Shortcuts suite (Start the daily / Continue / How's my streak / Start a gentle game) in Spotlight, Siri, Action button; **Focus filters** (hide the daily/streak during Work Focus — Focus that *adds* calm); interactive-widget growth (systemMedium "three moves" mode; per-channel widget configuration); Journaling Suggestions (completed daily as a private reflective moment). Mac: real menu bar with every command, menu-bar extra mini board, per-puzzle window restoration, ⌥-click why. Rejected: Siri voice solving (slower than the rose = demo-ware).
- **PRD-35 — The Store Tells the Story.** 2.0 re-launch: three preview videos (rose-on-remote; why-must-this-be-a-7; the comet), per-platform screenshot sets, localized store pages for all nine languages, custom product pages (learners → Coach; TV gaming → duel), in-app one-scroll "What's in 2.0" card. **Pricing intentionally deferred** — the covenant (no IAP/Pro/ads) is binding regardless; the free-update-plus-price-raise option from the product pass is recorded for the later decision.
- **[PRD-36](PRD-36.md) — Type That Scales.** Suite-wide Dynamic Type, inherited from PRD-20 Task 11 after that task was retired for measuring nothing: Nine has 0 `@ScaledMetric` / 0 `dynamicTypeSize` / 0 `relativeTo:`, so an AX5 sweep passes vacuously. `CouchTypography` is fixed points and is shared by all five Couch Suite apps, so giving it `relativeTo:` re-baselines every AX, contrast and screenshot lane in the suite at once — a suite decision, of the same kind as the CouchKit `HelpKit` strings deferred at `DEVIATIONS.md:2586`. Ships in three parts: the widget fixed-box bugs first (independent, reachable today — `BoardWidget.swift:129` grows into a `height: 34` frame with no `lineLimit`), then the CouchKit change, then a real AX5 lane calibrated against a deliberately clipped layout.

---

## Engineering foundations (Phase 0 — everything depends on this)

The single highest-leverage migration fix: **`nine.library` is one CouchStored blob — one undecodable `LibraryEntry` currently throws the array decode and discards the entire library.** Phase 0 ships, before any new enum case or record kind exists:
1. Tolerant-decode program: `Difficulty`/`Technique`/`GameKind` sentinel cases (`.beyond`, `.unrecognized`), lossy per-entry `BoardLibrary` decode; nothing ever throws out of a container decode. Old builds must *skip* (never delete) unknown CloudKit records — `LibraryCloudStore.projection` already returns nil-skip; add cross-version fixture tests to pin it.
2. **Golden determinism corpus**: ~50 (seed, difficulty) pairs → SHA-256 of encoded `GeneratedPuzzle` frozen. Any solver/topology refactor must keep classic hashes byte-identical — this is the tripwire that makes the variant refactor safe (risk #1: one reordered loop silently changes every future daily and breaks shared-seed duels).
3. Schema fixture harness (frozen blobs per released version, decoded both directions in CI; the PRD-17 downgrade drill scripted).
4. Additive `SolveStep` v2 fields, `LoggedMove.at`, string-catalog extraction.

Data placement: streak/prefs/history/CoachProgress stay KVS (small, LWW-safe; do not grow SolveHistory); library + immutable `SolveReplay` records in CloudKit zone `NineLibrary`; duel state in Game Center matchData (packed replay ≤64KB); pantry/catalogs local-only. Watch: KVS + WatchConnectivity daily hand-off (reuse `SharedDailyBoard` revision pattern) + on-watch steady-tier regeneration fallback; watch never generates above catalog-easy.

CI shape: Lane 1 Linux SwiftPM (Engine+Shared, every push) · Lane 2 macOS (all-platform builds, snapshot + AX-dump tests, contrast harness) · Lane 3 nightly (500–1000-puzzle variant proof soaks, catalog mining, pseudo-loc screenshots, downgrade drills, perf baselines: compose p95 per tier, rose round-trip <400ms, cold launch <800ms, widget ≤30MB).

## Craft charter (the bar, held program-wide)

Ten checkable standards (full detail from the craft pass, enforced in CI/release ritual): every interactive element a first-class AX citizen (≥44pt AX frames); contrast measured on the composited glass (96-cell theme×accent matrix); named motion tokens only — add `couchBloom` (the rose's promised overshoot lives here), `couchSettle`, `couchWhisper` to CouchKit, ban inline animation literals; latency budgets (bloom <250ms, flick-commit <50ms); zero misfires forever (1,000-flick soak per release); every string through catalogs, no truncation at top non-AX Dynamic Type; sensory sync law (visual → haptic ≤20ms → audio ≤1 frame); every state has a designed zero-state (honest absence over fake data); ProMotion discipline (120Hz bloom, 0fps idle board — the idle-pixel test); **one new input concept per release maximum**.

Anti-bloat constitution (2.0 will never): sell past the base price (no IAP/subscription/tips), show anything ad-shaped, gamify (XP/levels/badges/avatars), send notifications (single opt-in silent daily reminder at most, off by default), shame a streak, let the coach place a digit, add a fifth control button, play a sound identifiable by the person beside you in bed, or ship any pixel that asks for attention while the player is thinking. Taste ritual per feature: the 11pm-in-bed test, the roommate test, the first-flick test, the delete-it-for-a-week test, the idle-pixel test.

## Sequencing

- **Wave 0 (committed backlog):** PRD-6, 11–18 + loose ends (DualSense probe, pace-skew fix, CouchKit asks). PRD-11 is the Coach's UI substrate; PRD-12 the share substrate; PRD-14 the replay entry surface; PRD-17 stresses the technique chain PRD-23 generalizes.
- **Wave 1 — "Worthy" (1.5-class release):** Phase-0 engineering foundations + PRD-20 (l10n infra first), 19, 21, 22, 34. Long-lead start in parallel: PRD-23 engine work behind a debug channel.
- **Wave 2 — "Deeper":** PRD-25 Coach + School, PRD-26 Comet, PRD-31 iPad, PRD-33 Intents/Focus and PRD-30 Quiet Presence **all shipped**; killer engine soaks and PRD-27 duel remain.
- **Wave 3 — "Together" (the 2.0 launch train):** PRD-24 Channels ships (thermo first, then killer), PRD-27/28/29 Together features, PRD-35 store re-launch. **2.0 ships when Channels + Coach + one Together feature are simultaneously ready.**

## Top risks

1. Classic determinism breakage during the variant refactor → golden corpus first; classic paths delegate, never rewrite; BacktrackSolver originals frozen.
2. Schema evolution nuking user data (CouchStored discard-on-throw × version skew) → Phase 0 tolerance program before any new case exists; fixture drills forever.
3. Killer generation cost unshippable on-device → catalogs + forge pantry + attempt budgets; tier ships only at proven p95.
4. VoiceOver performance on an 81-element tree → stable identities, value-only updates, AX-dump perf checks.
5. Calm-brand erosion by accretion → the craft charter's constitution + taste ritual gate every PRD.

## Verification

- **This plan:** grounded in firsthand evidence — every "gap" was observed in the live build via sim-use (`/tmp/nine-audit/*.png`) or in source; every "asset" cites the file it lives in.
- **When execution begins (per wave):** golden-corpus hashes green before/after every engine PR; `describe-ui` AX diffs show the board's 81 elements + rotor actions; contrast harness passes the 96-cell matrix; sim-use walkthrough of each reshaped surface (shelf thumbnails, drawer grabber, first-run flow) on iPhone + iPad sims; `swift test` (Engine, Linux-clean) + nightly variant soaks; TestFlight builds via the existing fastlane lanes on all three platforms; the five-test taste ritual on every user-visible PRD before merge.

## Decisions taken (from user)

- Pricing: **deferred** — product planned independent of price; covenant binding.
- visionOS: **out of scope** (slot PRD-32 reserved).
- Deliverable now: **this plan document only**; PRD authoring / Wave-1 implementation are separate future requests.
